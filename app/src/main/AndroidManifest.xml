<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ady.ergo.tool.ergotool">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_ergotool"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_ergotool"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ChooseActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".Cat1Activity"
            android:launchMode="singleTask"/>
        <activity
            android:name=".Cat2Activity"
            android:launchMode="singleTask"/>
        <activity
            android:name=".Cat3Activity"
            android:launchMode="singleTask" />
        <activity android:name=".DynamicTry"/>
    </application>

</manifest>